<!DOCTYPE html>
<html>
<head>
  <title>HRC Pins For Sale</title>
  <link href="http://fonts.googleapis.com/css?family=PT+Sans" rel="stylesheet" type="text/css">
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>

<body>
  <header>
    <section id="logo">
      <%= link_to image_tag("logo_100x70wh.png"), root_path %>
    </section>
    <nav>
      <% if current_user %>
        <section class="signedin">
          <%= link_to "Sign Out", destroy_user_session_path, :method => :delete, class: "sign" %>
        </section>
          <p class="welcome">Welcome, <%= current_user.email %></p>
      <% else %>
        <section class="signinup">
          <%= link_to "Sign Up", new_user_registration_path, class: "sign" %>
          <%= link_to "Sign In", new_user_session_path, class: "sign" %>
        </section>
        <p class="membership">Currently, you only need membership to sell your pins, not to purchase them.</p>
      <% end %>
    </nav>
  </header>
  <section class="main">
    <section class="notices">
      <%= notice %>
      <%= alert %>
    </section>

    <%= yield %>

  </section>
</body>
</html>
